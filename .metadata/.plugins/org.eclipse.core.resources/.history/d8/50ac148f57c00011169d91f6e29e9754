package Hero;

import java.awt.Graphics2D;

class GameOver extends Scene
{
	//boolean spacePress = true;
	//boolean escapePress = true;
	
	float time;
	
	public GameOver()
	{
		//spacePress = true;
		//escapePress = true;
		//time = 0;
	}
	
	@Override
	public void update() 
	{
		time += Game.loopTime;
		System.out.println("time" + time);
		/*if(time > 1)
		{
			if(Keyboard.getInstance().isKeyPressed(KeyEvent.VK_SPACE))
			{
				if(spacePress == false)
				{
					//SceneManager.newLevel();
				}
			}
			else
			{
				spacePress = false;
			}
			
			if(Keyboard.getInstance().isKeyPressed(KeyEvent.VK_ESCAPE))
			{
				if(spacePress == false)
				{
					SceneManager.changeScene(0);
				}
			}
			else
			{
				spacePress = false;
			}
		}
		else
		{
			spacePress = true;
			escapePress = true;
			time+= Game.loopTime;
		}*/
		
		if(time >= 2)
		{
			SceneManager.changeScene(3);
		}
	}

	@Override
	public void draw(Graphics2D g2d) 
	{
		g2d.drawImage(LoadImages.getImage(2),0,0,null);
	}
}